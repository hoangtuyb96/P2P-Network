<%= form_for :like, url: likes_path, remote: true do |f| %>
  <div><%= hidden_field_tag :likeable_id, @object.id %></div>
  <div><%= hidden_field_tag :likeable_type, @object.class %></div>
  <%= f.submit t(".like_button"), class: "btn btn-primary" %>
<% end %>
<%= @object.likes.count %> likes
